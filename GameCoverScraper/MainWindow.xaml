<controls:MetroWindow x:Class="GameCoverScraper.MainWindow"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:scrapingRomCover="clr-namespace:GameCoverScraper"
                      xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
                      mc:Ignorable="d"
                      Title="Scraping ROM Cover"
                      MinWidth="900"
                      MinHeight="600"
                      WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

        <Style x:Key="ImageSuggestionButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="10" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{DynamicResource MahApps.Brushes.Gray9}"
                                BorderBrush="{DynamicResource MahApps.Brushes.Gray7}"
                                BorderThickness="1"
                                CornerRadius="5"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="1.0" ScaleY="1.0" />
                            </Border.RenderTransform>
                            <Border.Effect>
                                <DropShadowEffect Color="{DynamicResource MahApps.Colors.Gray5}" Direction="315"
                                                  ShadowDepth="3"
                                                  Opacity="0.4" BlurRadius="5" />
                            </Border.Effect>
                            <ContentPresenter x:Name="ButtonContentPresenter"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background"
                                        Value="{DynamicResource MahApps.Brushes.Accent4}" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush"
                                        Value="{DynamicResource MahApps.Brushes.Accent}" />
                                <Setter TargetName="ButtonBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="{DynamicResource MahApps.Colors.Accent}"
                                                          Direction="315"
                                                          ShadowDepth="5" Opacity="0.6" BlurRadius="8" />
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="ButtonBorder" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="1.02" ScaleY="1.02" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background"
                                        Value="{DynamicResource MahApps.Brushes.Accent}" />
                                <Setter TargetName="ButtonBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="Black" Direction="315" ShadowDepth="1" Opacity="0.7"
                                                          BlurRadius="2" />
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="ButtonContentPresenter" Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform X="1" Y="1" />
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="ImageCellTemplate">
            <Button Style="{StaticResource ImageSuggestionButtonStyle}" Click="SaveImage_Click">
                <StackPanel Width="{Binding ThumbnailWidth}" d:DataContext="{d:DesignInstance }">
                    <Image Width="{Binding ThumbnailWidth}" Height="{Binding ThumbnailHeight}"
                           Source="{Binding ImagePath}" d:DataContext="{d:DesignInstance }" />
                    <TextBlock HorizontalAlignment="Center" Text="{Binding ImageName}" FontWeight="Bold"
                               Margin="0,5,0,0" TextTrimming="CharacterEllipsis" ToolTip="{Binding ImageName}"
                               d:DataContext="{d:DesignInstance }" />
                    <TextBlock HorizontalAlignment="Center" Text="{Binding ImageFileSize}"
                               d:DataContext="{d:DesignInstance }" />
                    <TextBlock HorizontalAlignment="Center" d:DataContext="{d:DesignInstance }">
                        <TextBlock.Text>
                            <MultiBinding StringFormat="{}{0}x{1} ({2})">
                                <Binding Path="ImageWidth" />
                                <Binding Path="ImageHeight" />
                                <Binding Path="ImageEncodingFormat" />
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </StackPanel>
            </Button>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Menu Grid.Row="0" Name="MainMenu">
            <MenuItem Header="Theme">
                <MenuItem Header="Base Theme">
                    <MenuItem.Icon>
                        <Image Source="pack://application:,,,/images/theme.png" Width="16" Height="16" />
                    </MenuItem.Icon>
                    <MenuItem x:Name="LightTheme" Header="Light" Click="ChangeBaseTheme_Click" IsCheckable="True" />
                    <MenuItem x:Name="DarkTheme" Header="Dark" Click="ChangeBaseTheme_Click" IsCheckable="True" />
                </MenuItem>
                <MenuItem x:Name="MenuAccentColors" Header="Accent Colors">
                    <MenuItem.Icon>
                        <Image Source="pack://application:,,,/images/theme.png" Width="16" Height="16" />
                    </MenuItem.Icon>
                    <MenuItem x:Name="RedAccent" Header="Red" Click="ChangeAccentColor_Click" IsCheckable="True" />
                    <MenuItem x:Name="GreenAccent" Header="Green" Click="ChangeAccentColor_Click" IsCheckable="True" />
                    <MenuItem x:Name="BlueAccent" Header="Blue" Click="ChangeAccentColor_Click" IsCheckable="True" />
                    <MenuItem x:Name="PurpleAccent" Header="Purple" Click="ChangeAccentColor_Click" IsCheckable="True" />
                    <MenuItem x:Name="OrangeAccent" Header="Orange" Click="ChangeAccentColor_Click" IsCheckable="True" />
                    <MenuItem x:Name="LimeAccent" Header="Lime" Click="ChangeAccentColor_Click" IsCheckable="True" />
                    <MenuItem x:Name="EmeraldAccent" Header="Emerald" Click="ChangeAccentColor_Click"
                              IsCheckable="True" />
                    <MenuItem x:Name="TealAccent" Header="Teal" Click="ChangeAccentColor_Click" IsCheckable="True" />
                    <MenuItem x:Name="CyanAccent" Header="Cyan" Click="ChangeAccentColor_Click" IsCheckable="True" />
                    <MenuItem x:Name="CobaltAccent" Header="Cobalt" Click="ChangeAccentColor_Click" IsCheckable="True" />
                    <MenuItem x:Name="IndigoAccent" Header="Indigo" Click="ChangeAccentColor_Click" IsCheckable="True" />
                    <MenuItem x:Name="VioletAccent" Header="Violet" Click="ChangeAccentColor_Click" IsCheckable="True" />
                    <MenuItem x:Name="PinkAccent" Header="Pink" Click="ChangeAccentColor_Click" IsCheckable="True" />
                    <MenuItem x:Name="MagentaAccent" Header="Magenta" Click="ChangeAccentColor_Click"
                              IsCheckable="True" />
                    <MenuItem x:Name="CrimsonAccent" Header="Crimson" Click="ChangeAccentColor_Click"
                              IsCheckable="True" />
                    <MenuItem x:Name="AmberAccent" Header="Amber" Click="ChangeAccentColor_Click" IsCheckable="True" />
                    <MenuItem x:Name="YellowAccent" Header="Yellow" Click="ChangeAccentColor_Click" IsCheckable="True" />
                    <MenuItem x:Name="BrownAccent" Header="Brown" Click="ChangeAccentColor_Click" IsCheckable="True" />
                    <MenuItem x:Name="OliveAccent" Header="Olive" Click="ChangeAccentColor_Click" IsCheckable="True" />
                    <MenuItem x:Name="SteelAccent" Header="Steel" Click="ChangeAccentColor_Click" IsCheckable="True" />
                    <MenuItem x:Name="MauveAccent" Header="Mauve" Click="ChangeAccentColor_Click" IsCheckable="True" />
                    <MenuItem x:Name="TaupeAccent" Header="Taupe" Click="ChangeAccentColor_Click" IsCheckable="True" />
                    <MenuItem x:Name="SiennaAccent" Header="Sienna" Click="ChangeAccentColor_Click" IsCheckable="True" />
                </MenuItem>
            </MenuItem>
            <MenuItem x:Name="ThumbnailSizeMenu" Header="Set Thumbnail Size">
                <MenuItem Click="SetThumbnailSize_Click" Header="100 pixels" IsCheckable="True" />
                <MenuItem Click="SetThumbnailSize_Click" Header="150 pixels" IsCheckable="True" />
                <MenuItem Click="SetThumbnailSize_Click" Header="200 pixels" IsCheckable="True" />
                <MenuItem Click="SetThumbnailSize_Click" Header="250 pixels" IsCheckable="True" />
                <MenuItem Click="SetThumbnailSize_Click" Header="300 pixels" IsCheckable="True" />
                <MenuItem Click="SetThumbnailSize_Click" Header="350 pixels" IsCheckable="True" />
                <MenuItem Click="SetThumbnailSize_Click" Header="400 pixels" IsCheckable="True" />
                <MenuItem Click="SetThumbnailSize_Click" Header="450 pixels" IsCheckable="True" />
                <MenuItem Click="SetThumbnailSize_Click" Header="500 pixels" IsCheckable="True" />
            </MenuItem>
            <MenuItem x:Name="SearchEngineMenu" Header="Select Search Engine">
                <MenuItem Click="SetSearchEngine_Click" Header="Bing Web Search" Tag="BingWeb" IsCheckable="True" />
                <MenuItem Click="SetSearchEngine_Click" Header="Google Web Image Search" Tag="GoogleWeb" IsCheckable="True" />
                <!-- <MenuItem Click="SetSearchEngine_Click" Header="ZylaLabs (Bing) API" Tag="ZylaLabs" IsCheckable="True" /> -->
                <!-- <MenuItem Click="SetSearchEngine_Click" Header="Serp (Bing) API" Tag="Serp" IsCheckable="True" /> -->
                <!-- <MenuItem Click="SetSearchEngine_Click" Header="SerpHouse (Bing) API" Tag="SerpHouse" IsCheckable="True" /> -->
                <!-- <MenuItem Click="SetSearchEngine_Click" Header="Google API" Tag="Google" IsCheckable="True" /> -->
            </MenuItem>
            <MenuItem Header="Settings">
                <!-- <MenuItem Click="ApiSettings_Click" Header="API Settings"> -->
                <!--     <MenuItem.Icon> -->
                <!--         <Image Source="pack://application:,,,/images/theme.png" Width="16" Height="16" /> -->
                <!--     </MenuItem.Icon> -->
                <!-- </MenuItem> -->
                <MenuItem Click="ToggleDebugWindow_Click" x:Name="ToggleDebugWindow" Header="Show/Hide Log Window" IsCheckable="True" />
                <MenuItem x:Name="ToggleMameDescriptions" Header="Use MAME Descriptions" IsCheckable="True" Click="ToggleMameDescriptions_Click" />
            </MenuItem>
            <MenuItem Header="About">
                <MenuItem Click="DonateButton_Click" Header="Donate">
                    <MenuItem.Icon>
                        <Image Source="pack://application:,,,/images/donate.png" Width="16" Height="16" />
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem Click="ShowAboutWindow_Click" Header="About">
                    <MenuItem.Icon>
                        <Image Source="pack://application:,,,/images/about.png" Width="16" Height="16" />
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="Exit">
                <MenuItem Click="Exit_Click" Header="Exit">
                    <MenuItem.Icon>
                        <Image Source="pack://application:,,,/images/exit.png" Width="16" Height="16" />
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
        </Menu>


        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- LEFT COLUMN -->
            <Grid Grid.Column="0" Margin="0,0,10,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <GroupBox Grid.Row="0" Header="Select Folders &amp; Query" Margin="0,0,0,10">
                    <StackPanel Margin="5">
                        <Grid Margin="0,5,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="TxtRomFolder" Grid.Column="0"
                                     controls:TextBoxHelper.Watermark="Path to your ROM folder..." />
                            <Button Grid.Column="1" Content="Browse..." Margin="5,0,0,0"
                                    Click="BtnBrowseRomFolder_Click" />
                        </Grid>
                        <Grid Margin="0,5,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="TxtImageFolder" Grid.Column="0"
                                     controls:TextBoxHelper.Watermark="Path to your image folder..." />
                            <Button Grid.Column="1" Content="Browse..." Margin="5,0,0,0"
                                    Click="BtnBrowseImageFolder_Click" />
                        </Grid>
                        <Grid Margin="0,5,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" Content="Extra Query:" VerticalAlignment="Center" />
                            <TextBox Grid.Column="1" x:Name="TxtExtraQuery"
                                     controls:TextBoxHelper.Watermark="e.g. &quot;box art&quot;" />
                        </Grid>
                    </StackPanel>
                </GroupBox>

                <GroupBox Grid.Row="1" Margin="0,0,0,0">
                    <GroupBox.Header>
                        <StackPanel Orientation="Horizontal">
                            <Label x:Name="LabelMissingRoms" Content="LIST OF MISSING COVERS" Padding="0,0,5,0"
                                   Foreground="{DynamicResource MahApps.Brushes.IdealForeground}" />
                        </StackPanel>
                    </GroupBox.Header>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Button Grid.Row="0" Content="Check for Missing Images" Click="BtnCheckForMissingImages_Click"
                                Margin="5" />
                        <Grid Grid.Row="1">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <ListBox x:Name="LstMissingImages" SelectionChanged="LstMissingImages_SelectionChanged"
                                         BorderThickness="0">
                                    <ListBox.ContextMenu>
                                        <ContextMenu>
                                            <MenuItem Header="Remove Item from the list" Click="RemoveItemFromList_Click" x:Name="ContextMenuRemoveItem" />
                                            <MenuItem Header="Copy FileName" Click="CopyFileName_Click" x:Name="ContextMenuCopyFileName" />
                                        </ContextMenu>
                                    </ListBox.ContextMenu>
                                </ListBox>
                            </ScrollViewer>
                        </Grid>
                        <Button Grid.Row="2" x:Name="BtnRemoveSelectedItem" Content="Remove Selected Item from List"
                                Click="BtnRemoveSelectedItem_Click" Margin="5" />
                    </Grid>
                </GroupBox>
            </Grid>

            <!-- RIGHT COLUMN -->
            <GroupBox Grid.Column="1" Header="Image Suggestions">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Label x:Name="LblSearchQuery" Grid.Row="0" Margin="5,0,5,5" />

                    <Grid Grid.Row="1">
                        <ScrollViewer x:Name="ImageScrollViewer" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding PanelImages}" d:DataContext="{d:DesignInstance }">
                                <ItemsControl.ItemTemplate>
                                    <StaticResource ResourceKey="ImageCellTemplate" />
                                </ItemsControl.ItemTemplate>
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </ScrollViewer>

                        <wv2:WebView2 x:Name="WebView" Visibility="Collapsed" />

                        <controls:ProgressRing IsActive="{Binding IsSearching}" HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               d:DataContext="{d:DesignInstance }" />

                        <TextBlock Text="Select a missing cover from the list to see suggestions."
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Foreground="{DynamicResource MahApps.Brushes.Gray5}"
                                   Visibility="{Binding IsSearching, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=inverse}"
                                   d:DataContext="{d:DesignInstance }">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition
                                                    Binding="{Binding ElementName=ImageScrollViewer, Path=DataContext.(scrapingRomCover:MainWindow.PanelImages).Count}"
                                                    Value="0" />
                                                <Condition Binding="{Binding IsSearching}" Value="False" />
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Visibility" Value="Visible" />
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>
                </Grid>
            </GroupBox>
        </Grid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2" Background="{DynamicResource MahApps.Brushes.Gray8}">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>

            <StatusBarItem Grid.Column="0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Search Engine: " Foreground="{DynamicResource MahApps.Brushes.Text}" />
                    <TextBlock x:Name="StatusSearchEngine" Text="{Binding SearchEngineDisplay}" FontWeight="Bold"
                               Foreground="{DynamicResource MahApps.Brushes.Text}" d:DataContext="{d:DesignInstance }" />
                </StackPanel>
            </StatusBarItem>

            <StatusBarItem Grid.Column="1" HorizontalAlignment="Center">
                <TextBlock x:Name="StatusMessage" Text="Ready" Foreground="{DynamicResource MahApps.Brushes.Text}" />
            </StatusBarItem>

            <StatusBarItem Grid.Column="2">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Images: " Foreground="{DynamicResource MahApps.Brushes.Text}" />
                    <TextBlock x:Name="StatusImageCount" Text="0" FontWeight="Bold"
                               Foreground="{DynamicResource MahApps.Brushes.Text}" />
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</controls:MetroWindow>